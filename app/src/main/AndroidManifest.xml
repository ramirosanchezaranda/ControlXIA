<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Permite abrir apps / disparar acciones con la pantalla bloqueada o en
         segundo plano (background activity start). Opcional: sin esto, abrir
         apps solo funciona con ControlXIA en primer plano. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 11+: declarar las consultas de paquetes que hacemos. -->
    <queries>
        <!-- Servicio de reconocimiento de voz (SpeechRecognizer). -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <!-- Apps con ícono de lanzador, para resolver "abrí <app>" por nombre. -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".XiaApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlXIA">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:showWhenLocked="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Servicio de escucha persistente. El tipo "microphone" es obligatorio
             para poder usar el micrófono de forma continua (Android 14+ además
             exige el permiso FOREGROUND_SERVICE_MICROPHONE de arriba). -->
        <service
            android:name=".service.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Relanza el servicio cuando el teléfono se reinicia. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Watchdog: alarma periódica que revive el servicio si el sistema lo mató. -->
        <receiver
            android:name=".service.WatchdogReceiver"
            android:exported="false" />

    </application>
</manifest>
